# Backend
backend/target/
backend/.mvn/
*.class
*.jar
*.war

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env.local

# Secrets — never commit real API keys / passwords (keep the .example templates)
secrets.properties
backend/secrets.properties
**/secrets.properties
.env
**/.env
!.env.example
!**/.env.example

# This repo is PRIVATE — the local dev keys are committed here for cross-device
# convenience. IMPORTANT: before ever making this repo public, delete the line
# below, rotate the Adzuna key, and stop tracking backend/secrets.properties.
!backend/secrets.properties

# Docker
*.log

# IDE
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
